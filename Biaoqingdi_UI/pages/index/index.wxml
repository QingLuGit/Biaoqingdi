<!--index.wxml-->
<view class="container">
  <view class="title">
    <title>{{motto}}</title>
  </view>
  <view class="startbtn">
      <button type="primary" bindtap="bindStart">
        开始   
      </button>
      <!-- 
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
      -->
  </view>
  
</view>
